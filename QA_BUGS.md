# QA Аудит ВЕРТА — 2026-07-01

## Повторный аудит — 2026-07-01 (после фиксов, коммит 91e5e3e)

Статический аудит кода (без запуска сервера, без обращения к БД). Проверены все файлы, которые разработчик заявил как изменённые: `checkin/actions.ts`, `habits/bulk/actions.ts`, `medical/actions.ts`, `training/actions.ts`, `lib/data.ts`, `register/actions.ts`, миграция `031_sleep_log_user_isolation.sql`. Пометки [ИСПРАВЛЕНО ✓] / [ЧАСТИЧНО / ЕСТЬ ЗАМЕЧАНИЕ] расставлены по каждому P0/P1 ниже, в оригинальных секциях. P2 не трогались — актуальны как есть.

**Общий вывод: все 5 P0 подтверждены закрытыми, новых P0/P1 в самих фиксах не найдено.** Один нюанс (не баг, а архитектурная заметка) — см. новый пункт в конце P1.

Статический аудит кода (без запуска сервера, без обращения к БД). Ветка `feat/multiuser` (только что влит большой мультиюзер-рефакторинг). Проверены файлы из `web/web/lib/` и `web/web/app/(app)/{checkin,medical,training,habits,import,insights,dashboard}`, `web/web/app/login`, `web/web/app/register`, плюс сверка со схемой миграций в `web/supabase/migrations/`.

**Важный контекст безопасности**: в проекте включён RLS на таблицах, но единственная реально работающая политика (`010_balance_wheel.sql`) — это `using (true) with check (true)` для service_role. Приложение везде использует `supabaseAdmin()` (service_role), который **обходит RLS полностью**. Это значит, что изоляция пользователей на 100% держится на самодельном хелпере `byUser(query, uid)` в каждом запросе — если разработчик забыл его вызвать, БД не спасёт. Отсюда P0 находки ниже.

## Резюме
P0: 5 | P1: 6 | P2: 6 | Итого: 17
Общая оценка: мультиюзер-миграция сделана в целом аккуратно (паттерн `byUser`/`toUid` применён почти везде), но есть несколько мест, где фильтр по `app_user_id` пропущен полностью — это прямые межпользовательские утечки данных, а не гипотетические. Дополнительно один `redirect()` в `try/catch` (глотается) и один upsert с несуществующим constraint (сломан для всех пользователей на конкретной странице). Рекомендую не мержить в `main`, пока не закрыты все P0.

**Обновление 2026-07-01 (повторный аудит):** все 5 P0 и все 6 P1 перепроверены после фиксов коммита `91e5e3e`. 10 из 11 подтверждены закрытыми полностью ([ИСПРАВЛЕНО ✓]). 1 из 11 ("days ago" в UTC) отмечен [ЧАСТИЧНО / ЕСТЬ ЗАМЕЧАНИЕ] — код исправлен корректно, но эффект на проде под вопросом из-за отсутствия явного `TZ` в конфиге деплоя (Vercel по умолчанию UTC). Найдено 2 новые мелкие P2 (не блокеры). Подробности — пометки внутри каждого пункта ниже.

## P0 — Блокеры

**[P0] `saveBulkHabits` не фильтрует по пользователю вообще**
Файл: `web/web/app/(app)/habits/bulk/actions.ts:21-47`
Проблема: Функция читает `daily_log` через `select("*").in("log_date", dates)` без какого-либо `app_user_id` фильтра, а затем делает `upsert(..., { onConflict: "log_date" })`. При этом миграция `021_user_isolation.sql` заменила уникальный констрейнт `daily_log_log_date_key` на `daily_log_user_date_key UNIQUE(app_user_id, log_date)` — констрейнта `log_date` больше не существует.
Воспроизведение: (а) Любой пользователь, открывший `/habits/bulk`, при сохранении читает и подмешивает в апдейт **чужие** daily_log строки за те же даты (первая попавшаяся по `log_date`, независимо от владельца) — реальная утечка/порча данных между пользователями. (б) `onConflict: "log_date"` ссылается на несуществующий unique constraint → Supabase вернёт ошибку `there is no unique or exclusion constraint matching the ON CONFLICT specification` на каждый сохранение — фича, вероятно, полностью не работает после мультиюзер-миграции.
Рекомендация: Добавить `getAppUserId()`/`byUser()` в `select` и `upsert`, поставить `app_user_id` в каждую строку payload, исправить `onConflict` на `"app_user_id,log_date"`.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/habits/bulk/actions.ts:34-66` (2026-07-01). `select("*").in("log_date", dates)` теперь обёрнут в `byUser(..., uid)` — читает только свои строки. `upsertRows` явно ставит `app_user_id: uid` в каждую строку payload. `onConflict` исправлен на `"app_user_id,log_date"`, что совпадает с реальным констрейнтом `daily_log_user_date_key` из 021. Есть аккуратная деталь: код деструктурирует `id` из `existingByDate[date]` и не включает его в payload (`const { id: _id, ...base } = ...`) — верно, иначе upsert пытался бы обновить PK напрямую, что могло дать конфликт. Edge case проверен: для legacy пользователя (`uid=null`) `byUser` делает `.is("app_user_id", null)`, что соответствует backfill из 021 (строки Марины получили её UUID, а не остались NULL — см. ниже про `getCurrentUser`, поэтому для реального продакшн-сценария Марина всегда будет иметь `uid`=её UUID, не null; ветка `null` актуальна только для гипотетического юзера без БД-сессии). Легитимный сценарий (обычный пользователь сохраняет привычки за месяц) не ломается — фильтрация в `select` и `upsert` используют один и тот же `uid`.

**[P0] `deleteMed` удаляет препарат без проверки владельца**
Файл: `web/web/app/(app)/checkin/actions.ts:99-107`
Проблема: `db.from("medication").delete().eq("id", id)` — нет `byUser`/`app_user_id` фильтра. При этом для сидовых препаратов id — общеизвестные строки (`sumatriptan`, `nurofen` и т.п.), одинаковые у всех пользователей.
Воспроизведение: Любой пользователь может передать в `deleteMed` id чужого препарата (в т.ч. предсказуемый seed-id) и удалить запись, которая физически принадлежит другому аккаунту (или общей seed-записи, которую видят/использует другой пользователь).
Рекомендация: Добавить `byUser(db.from("medication").delete().eq("id", id), uid)`.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/checkin/actions.ts:105-114` (2026-07-01). `deleteMed` теперь делает `byUser(db.from("medication").delete().eq("id", id), uid)` — ровно та рекомендация, что была дана. Edge case: если пользователь A (uid=X) передаёт seed-id чужого/общего препарата, `byUser` добавит `.eq("app_user_id", X)` к запросу на удаление — строка с `app_user_id != X` (или NULL, если это старый сид) не совпадёт, `delete()` затронет 0 строк, но Supabase **не вернёт ошибку** в этом случае (успешный delete с 0 affected rows) — UI покажет "ok: true", хотя ничего не удалилось. Это не баг безопасности (чужие данные защищены), но небольшая проблема UX/наблюдаемости: пользователь может решить, что препарат удалён, хотя удалилась просто "не найденная своя" запись. Не блокер, отметил как P2-наблюдение ниже. Легитимный сценарий (удаление своего кастомного препарата, созданного через `createMed` с `app_user_id: uid`) работает корректно.

**[P0] `getSignedUrl` не проверяет владельца файла**
Файл: `web/web/app/(app)/medical/actions.ts:127-132`
Проблема: Функция принимает произвольный `filePath: string` и выдаёт подписанный URL на 1 час без всякой проверки, что этот файл принадлежит текущему пользователю (`db.storage.from("medical-records").createSignedUrl(filePath, 3600)`).
Воспроизведение: `getSignedUrl` — экспортируемый server action (доступен клиенту как RPC). Зная (или подобрав/увидев в чужой сессии) путь вида `records/<uid>/<ts>_<filename>`, любой авторизованный пользователь получает подписанную ссылку на скачивание чужого медицинского документа. Сейчас функция вызывается только из `getRecords()` (которая уже отфильтрована по `byUser`), поэтому прямо сейчас эксплуатация требует вызова экспортированного action напрямую (через devtools/curl к RSC action endpoint) — но это именно тот случай, где нельзя полагаться на "просто из UI так не вызвать".
Рекомендация: Перед созданием signed URL проверить, что `filePath` начинается с `records/${uid}/` (или сделать выборку из `medical_record` по `file_path` + `byUser` и только затем подписывать).

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/medical/actions.ts:127-142` (2026-07-01). Реализован именно рекомендованный вариант: перед подписью URL идёт `byUser(db.from("medical_record").select("id").eq("file_path", filePath), uid).limit(1).maybeSingle()` — если запись не найдена (не принадлежит текущему uid), функция возвращает `null` без обращения к Storage вообще. Легитимный сценарий проверен: `medical/page.tsx:59-70` вызывает `getRecords()` (уже отфильтрован через `byUser`) и затем `getSignedUrl(r.file_path!)` для каждой своей же записи — `file_path` гарантированно принадлежит текущему uid, второй запрос в `getSignedUrl` пройдёт по тому же условию `byUser` и найдёт ту же строку → скачивание работает. Атака "подобрать чужой filePath и вызвать getSignedUrl напрямую как RPC" теперь блокируется на уровне проверки владения. Уязвимость закрыта именно так, как описано в исходной находке.

**[P0] `workout_exercise` не имеет `app_user_id` и модифицируется без проверки владения `workout_id`**
Файл: `web/web/app/(app)/training/actions.ts:99` (`saveExercises`), `lib/data.ts:429-438` (`getWorkoutExercises`)
Проблема: Таблица `workout_exercise` (см. `web/supabase/migrations/006_training_exercises.sql`) вообще не содержит колонку `app_user_id`. `saveExercises(workoutId, exercises)` делает `delete().eq("workout_id", workoutId)` и `insert(rows)` без проверки, что `workoutId` принадлежит текущему пользователю. `getWorkoutExercises` тоже читает без фильтра.
Воспроизведение: Если пользователь A узнаёт/подбирает `workout_id` (UUID) пользователя B (например, через утечку в другом месте или общий шаблон), пользователь A может стереть и переписать чужие данные об упражнениях тренировки.
Рекомендация: Добавить `app_user_id` в `workout_exercise` (новая миграция) либо всегда джойнить через `workout_log.app_user_id` перед мутацией — сначала убедиться `SELECT id FROM workout_log WHERE id = workoutId AND app_user_id = uid`, и только потом удалять/вставлять exercises.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/training/actions.ts:92-120` (`saveExercises`) и `web/lib/data.ts:440-458` (`getWorkoutExercises`) (2026-07-01). Оба места теперь делают именно рекомендованную проверку: `byUser(db.from("workout_log").select("id").eq("id", workoutId), uid).maybeSingle()` перед любой мутацией/чтением `workout_exercise`. Если `owned` не найден — `saveExercises` возвращает `{ ok: false, error: "Тренировка не найдена" }` до `delete`/`insert`; `getWorkoutExercises` возвращает `[]` до `select`. Легитимный сценарий (владелец редактирует свою тренировку) не ломается — `owned` найдётся, дальнейший `delete().eq("workout_id", workoutId)` + `insert(rows)` выполняются как раньше. Комментарий в коде честно фиксирует, почему нужен именно такой обходной путь («workout_exercise has no app_user_id column»), это не забыто на будущее.

**[P0] `sleep_log` — глобальная таблица без app_user_id, читается всеми пользователями**
Файл: `lib/data.ts:491-503` (`getRecentSleepData`), схема: `web/supabase/migrations/015_ringconn_wearable.sql:12-23`
Проблема: Таблица `sleep_log` не имеет колонки `app_user_id` вообще. `getRecentSleepData` не вызывает `byUser` — прямой `db.from("sleep_log").select(...)`.
Воспроизведение: Любой зарегистрированный пользователь ВЕРТА, открывший экран с данными сна (wearable/insights), увидит данные сна Марины (сид-данные RingConn), потому что фильтрации по пользователю физически не может быть — колонки нет.
Рекомендация: Добавить `app_user_id` в `sleep_log` миграцией, проставить существующим строкам id первого пользователя (аналогично `021_user_isolation.sql`), обновить `getRecentSleepData` на `byUser`.

**[ИСПРАВЛЕНО ✓]** Перепроверено — миграция `supabase/migrations/031_sleep_log_user_isolation.sql` и `web/lib/data.ts:511-526` (2026-07-01). Миграция 031 в точности повторяет паттерн 021/024/029: `ALTER TABLE sleep_log ADD COLUMN IF NOT EXISTS app_user_id uuid REFERENCES app_user(id)`, backfill через `DO $$ ... SELECT id INTO first_user_id FROM app_user ORDER BY created_at ASC LIMIT 1; UPDATE sleep_log SET app_user_id = first_user_id WHERE app_user_id IS NULL; END $$`, плюс добавлен индекс `sleep_log_user_date_idx ON sleep_log (app_user_id, log_date)` — уместное дополнение, которого не было в более старых миграциях. `getRecentSleepData` теперь оборачивает запрос в `byUser(..., uid)`. Замечание не по коду, а по данным: в сид-данных из `015_ringconn_wearable.sql` есть дни с двумя записями сна на одну `log_date` (17 и 24 июня — очевидно, дневной сон + ночной), в `sleep_log` нет уникального констрейнта на `(app_user_id, log_date)`, так что это не создаёт конфликтов при миграции — упомянуто для полноты, не блокер. Легитимный сценарий (пользователь видит собственные данные сна на /wearable) не пострадал.

## P1 — Важные баги

**[P1] `redirect()` внутри `try/catch` — эффективно проглатывается**
Файл: `web/web/app/register/actions.ts:13-32`
Проблема: `redirect()` в Next.js бросает исключение `NEXT_REDIRECT`, которое ловится тем же `try/catch`, что оборачивает `createUser`. В частности, `redirect("/onboarding")` на строке 16 находится **внутри** `try` — если он выполнится успешно, это нормально (redirect должен пробросить исключение наружу из функции), но конструкция хрупкая: любое исключение из кода после `redirect()` (например, второй `createUser`/`setAuthCookie` внутри вложенного `try` на строках 22-29) тоже полагается на то, что redirect добрасывается через несколько уровней catch. Второй вложенный `try/catch` (строки 22-29) ловит **всё**, включая потенциальный `NEXT_REDIRECT` от `redirect("/onboarding")` на строке 25, и в catch-блоке просто делает ещё один `redirect("/register?e=2")` — то есть если после успешного `createUser`/`setAuthCookie` redirect на `/onboarding` по какой-то причине попадает в catch (например, Next меняет поведение или добавляется код между строками), пользователь молча не увидит /onboarding.
Воспроизведение: Пограничный сценарий "email уже существует, но онбординг не завершён" → `deleteUnfinishedUser` → повторный `createUser` → `setAuthCookie` → `redirect("/onboarding")` — весь этот блок обёрнут в `try` с `catch` который перехватывает `NEXT_REDIRECT`. Технически Next.js специально не считает `NEXT_REDIRECT` ошибкой в самом верхнем вызывающем коде, но вложенность try/catch здесь избыточна и рискованна — при рефакторинге (например, добавлении `await` кода после redirect) это сломается тихо.
Рекомендация: Не оборачивать `redirect()` в try/catch. Разделить: сначала выполнить всю бизнес-логику (create/delete/create) в try/catch без redirect внутри, затем вызывать `redirect()` один раз после успешного получения `user`, вне какого-либо catch.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/register/actions.ts:1-39` (2026-07-01). Именно рекомендованная структура: `redirect()` вызывается ровно один раз, в самом конце функции, вне какого-либо `try/catch`. Внутри `try/catch` (и вложенного `catch`) теперь только присваивание переменной `target` (строка) — исключения из бизнес-логики (`createUser`/`setAuthCookie`/`deleteUnfinishedUser`) ловятся и превращаются в целевой путь, но сам `redirect(target)` вызывается уже после выхода из обоих try/catch блоков, поэтому `NEXT_REDIRECT` не может быть случайно проглочен. Единственная оставшаяся деталь (не баг): `redirect("/register?e=3")` на строке 11 (проверка длины пароля) — это ранний return-стиль redirect, он не в try/catch и корректен сам по себе. Логика полностью соответствует тому, что было рекомендовано.

**[P1] `getMonthHabitStats` падает на месяцах короче 31 дня**
Файл: `lib/data.ts:319-333`
Проблема: `.gte("log_date", `${ym}-01`).lte("log_date", `${ym}-31`)` — для февраля/апреля/июня/сентября/ноября `${ym}-31` — невалидная дата (`2026-02-31`). PostgREST/Postgres кастует строковый литерал в тип `date` и упадёт с ошибкой на невалидной дате. Код не проверяет `error` (`const { data } = await byUser(...)`), поэтому ошибка проглатывается молча, `rows = []`, функция тихо возвращает `{ done: 0, daysLogged: 0 }`.
Воспроизведение: На дашборде для любого месяца с 28/29/30 днями (например, текущий июль 2026 не пострадает, а вот показатели за февраль или сентябрь всегда будут "0% за месяц"), статистика привычек за месяц будет занижена/обнулена без видимой ошибки.
Рекомендация: Использовать корректную верхнюю границу — первый день следующего месяца через `lt`, как уже сделано в `getTriptanCount` (строки 89-93 того же файла, где есть правильный паттерн nextY/nextM).

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/lib/data.ts:324-344` (2026-07-01). `getMonthHabitStats` теперь вычисляет `end` как первый день следующего месяца (`nextY`/`nextM` — тот же паттерн, что в `getTriptanCount`) и использует `.gte(start).lt(end)` вместо сломанного `lte(ym-31)`. Проверил вручную для декабря (`m===12` → `nextY=y+1, nextM=1`) и февраля (`2026-02` → `end=2026-03-01`) — граница корректна в обоих случаях, включая переход через год. Баг был именно в невалидной дате `${ym}-31` для месяцев короче 31 дня — теперь такой строки нет вообще.

**[P1] `getRecentWearableData` / `getRecentSleepData` считают "days ago" в UTC**
Файл: `lib/data.ts:449-458` и `491-499`
Проблема: `const since = new Date(); since.setDate(since.getDate() - days); ... since.toISOString().slice(0,10)`. `toISOString()` всегда возвращает UTC-дату. Для пользователя в часовом поясе UTC+3 (Россия) в промежутке 00:00–03:00 локального времени это даст дату "вчера" по UTC, сдвигая окно на день раньше, чем ожидается.
Воспроизведение: Между полночью и 3 утра по Москве окно "последние 7/28 дней" будет заканчиваться на день раньше, чем должно — минимальный, но систематический сдвиг данных wearable/sleep.
Рекомендация: Вычислять `since` в локальной таймзоне пользователя (например, через `startOfDay`/локальные Y-M-D компоненты как в `lib/cycle.ts`), а не `toISOString()`.

**[ЧАСТИЧНО / ЕСТЬ ЗАМЕЧАНИЕ]** Перепроверено в `web/lib/data.ts:32-34` (`isoLocal`) и в местах вызова `469-499` (`getRecentWearableData`), `511-526` (`getRecentSleepData`) (2026-07-01). Добавлена функция `isoLocal(d)`, которая строит `YYYY-MM-DD` из `d.getFullYear()/getMonth()/getDate()` вместо `since.toISOString().slice(0,10)`. Проблема: `getFullYear()/getMonth()/getDate()` без аргументов берут часовой пояс **окружения выполнения (`process.env.TZ` / TZ рантайма Node.js)**, а не пользователя. Локально (dev-машина, скорее всего `TZ` не задан явно и наследуется от ОС — может быть MSK) фикс действительно уберёт сдвиг. Но проверил: в проекте нет `vercel.json`, `.env` с `TZ=`, никакой явной установки часового пояса — а Vercel serverless-функции по умолчанию выполняются в **UTC**. Это значит на проде `d.getFullYear()/getMonth()/getDate()` в `isoLocal` фактически вернут те же UTC-компоненты, что и `toISOString()` — то есть сдвиг для пользователей в МСК (UTC+3) между 00:00 и 03:00 **скорее всего не устранён на проде**, хотя код теперь выглядит "локально корректным". Нужна проверка: либо явно завести `TZ=Europe/Moscow` в конфиге деплоя, либо жёстко прибавлять фиксированный offset в `isoLocal` вместо того чтобы полагаться на TZ окружения. Рекомендую подтвердить на реальном проде (залогировать `new Date().getTimezoneOffset()` в API route) прежде чем закрывать эту находку окончательно.

**[P1] `deleteWorkout` / `bulkDeleteWorkouts` удаляют `workout_exercise` без проверки владельца**
Файл: `web/web/app/(app)/training/actions.ts:123-135` (`deleteWorkout`), `198-211` (`bulkDeleteWorkouts`)
Проблема: Обе функции сначала делают `db.from("workout_exercise").delete().eq("workout_id", id)` (или `.in("workout_id", ids)`) без всякой проверки владения, и только потом `workout_log` удаляется через `byUser`. Если `id` не принадлежит текущему пользователю, `workout_log` не удалится (RLS/byUser сработает), но связанные `workout_exercise` для **чужой** тренировки уже будут удалены до этой проверки.
Воспроизведение: Пользователь A вызывает `deleteWorkout(id)` с `id` чужой (пользователя B) тренировки. Строка `workout_log` пользователя B останется (фильтр `byUser` не даст её удалить), но её `workout_exercise` (все подходы/повторы/веса) будут стёрты, потому что удаление exercises не фильтруется по владельцу вообще.
Рекомендация: Сначала проверить владение через `byUser` на `workout_log` (или джойн), и только если подтверждено — удалять `workout_exercise`. Либо: выполнять удаление exercises только если `workout_log` delete подтвердил затронутую строку.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/training/actions.ts:131-151` (`deleteWorkout`) и `214-236` (`bulkDeleteWorkouts`) (2026-07-01). `deleteWorkout` теперь делает `byUser(workout_log.select("id").eq("id", id), uid).maybeSingle()` первым делом и возвращает `{ ok: false }` до касания `workout_exercise`, если `owned` не найден. `bulkDeleteWorkouts` идёт ещё аккуратнее: сначала `byUser(workout_log.select("id").in("id", ids), uid)` — вычисляет `ownedIds` (пересечение запрошенных id с тем, что реально принадлежит uid), и удаляет `workout_exercise`/`workout_log` только для `ownedIds`, а не для всех `ids`. Это правильно закрывает и частичный случай: если в batch-запросе часть id принадлежит другому пользователю, для них `workout_exercise` не тронется вообще. Легитимный сценарий (владелец удаляет свою тренировку/несколько своих тренировок) работает как раньше.

**[P1] `getWorkoutTemplates`/`getWeeklySchedule` помечены "global shared" в комментарии, но фильтруются `byUser`**
Файл: `lib/data.ts:398-427`
Проблема: Комментарий гласит `// Global shared templates — no per-user filtering`, но код тут же передаёт запрос через `byUser(..., uid)`, то есть реально фильтрует по `app_user_id`. Для пользователей без собственных шаблонов (`uid` — новый UUID, ни одной строки с этим `app_user_id`) `getWorkoutTemplates()` вернёт **пустой список**, а не общие сидовые шаблоны, потому что `byUser` для не-legacy uid делает `.eq("app_user_id", uid)`, что не совпадёт с NULL-строками сидовых шаблонов.
Воспроизведение: Новый зарегистрированный пользователь открывает `/training` → создаёт тренировку типа "Силовая" → `findTemplate` ищет по `templates` (полученным из `getWorkoutTemplates()`) → список пуст → упражнения не подставляются автоматически, хотя по замыслу (согласно комментарию) должны быть общими для всех.
Рекомендация: Либо действительно сделать выборку без фильтра пользователя (раз комментарий говорит "global"), либо, если объединение сид-шаблонов (`app_user_id IS NULL`) и пользовательских — задумано, использовать `.or(app_user_id.is.null,app_user_id.eq.${uid})` вместо `byUser`.

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/lib/data.ts:409-427` (2026-07-01). Реализован ровно рекомендованный второй вариант: комментарий обновлён («Templates are shared: seed rows have app_user_id IS NULL, plus any user's own custom templates»), а запрос теперь ветвится — `uid ? query.or(`app_user_id.is.null,app_user_id.eq.${uid}`) : query.is("app_user_id", null)`. Легитимный сценарий, ранее сломанный (новый пользователь без своих шаблонов получал пустой список), теперь работает: новый uid увидит сид-шаблоны (`app_user_id IS NULL`) через `.or(...)`. Мелкое замечание не блокирующее: `uid` интерполируется напрямую в строку `.or()` без экранирования — `uid` в этом контексте всегда UUID из `getCurrentUser()` (или null), так что инъекция маловероятна, но стоит иметь в виду если формат `uid` когда-либо станет менее строгим.

**[P1] Missing `router.refresh()` после `deleteMed`/`createMed` в UI — нужно перепроверить связанный компонент вручную**
Файл: `web/web/app/(app)/checkin/actions.ts:74-107` (проверить вызывающий компонент, не входящий в список аудита)
Проблема: Оба action используют `revalidatePath("/checkin")`/`revalidatePath("/dashboard")`, что инвалидирует серверный кеш, но если вызывающий клиентский компонент не делает `router.refresh()` после успешного ответа, список препаратов на экране может не обновиться сразу (Router Cache клиента живёт 30 сек по умолчанию).
Воспроизведение: Не проверено напрямую (компонент вне списка аудита), но паттерн risky — стоит проверить компонент, который рендерит список meds на `/checkin`, на предмет `router.refresh()` после `createMed`/`deleteMed`.
Рекомендация: Убедиться, что клиентский компонент со списком meds вызывает `router.refresh()` сразу после успешного `createMed`/`deleteMed` (по аналогии с `upload-form.tsx:78` и `bulk-form.tsx:82`, где это сделано правильно).

**[ИСПРАВЛЕНО ✓]** Перепроверено в `web/app/(app)/checkin/checkin-form.tsx` (2026-07-01) — не было в списке файлов, заявленных разработчиком как изменённые, но так как P1 явно требовал перепроверки именно этого компонента, проверил отдельно. `router.refresh()` вызывается сразу после `createMed(...)` (строка 105) и сразу после `deleteMed(id)` (строка 117) — список meds на `/checkin` обновится немедленно, Router Cache клиента (30с) не помешает. Находка закрыта.

## P2 — Улучшения и edge cases

**[P2] `getAllHabits` двойной запрос при ошибке — неэффективно, но не баг**
Файл: `lib/data.ts:172-194`
Проблема: При ошибке первого запроса (например, отсутствие колонок `started_month`/`ended_month` на старой БД) делается второй запрос-фоллбэк. Рабочий код, но означает, что при систематической проблеме (не хватает колонки) каждый вызов делает 2 похода в БД.
Рекомендация: Если фоллбэк больше не нужен (миграции применены), убрать; если нужен — оставить с комментарием почему.

**[P2] `previewCSV`/`importCSV` дублируют парсинг и не используют одинаковый `uid`-подход для превью**
Файл: `web/web/app/(app)/import/migrebot/actions.ts:68-94`
Проблема: `previewCSV` не привязан к пользователю (это ОК, т.к. просто парсинг для UI), но стоит убедиться, что `previewCSV` не сохраняет ничего в БД по ошибке в будущем рефакторинге — сейчас безопасно, но нет теста/защиты.
Рекомендация: Добавить unit-тест на `previewCSV`, что он не делает никаких side-effects.

**[P2] `MedCalendar`/`med-calendar.tsx` — эвристика распознавания медикаментов по regex может давать ложные срабатывания**
Файл: `web/web/app/(app)/checkin/meds/med-calendar.tsx:35-61`
Проблема: `MED_PATTERNS` использует простые regex по подстроке (например `/спрей|назальн/i`). Если в свободнотекстовом поле `meds` пользователь напишет что-то вроде "не помог спрей от насморка", это засчитается как приём `nasal_spray`, хотя мигренозного спрея не было.
Рекомендация: Не блокер (это UX-эвристика для отображения истории), но стоит задокументировать false-positive риск или показывать оригинальный текст рядом (что уже частично сделано через `MigreBotSection`).

**[P2] `toWeeks`/`buildDayRange` в `med-calendar.tsx` — потенциальный тяжёлый цикл на больших диапазонах**
Файл: `web/web/app/(app)/checkin/meds/med-calendar.tsx:8-19`
Проблема: `buildDayRange` генерирует посуточный массив (`while (cur <= to)`), а `addDays` каждый раз создаёт новый `Date` через `new Date(iso + "T12:00:00")`. Для 3-месячного окна (~90 дней) это не проблема, но если когда-нибудь диапазон расширят (например, "весь год"), будет 365 итераций с созданием Date объектов — не критично, но стоит иметь в виду при расширении фичи.
Рекомендация: Не менять сейчас, отметить как потенциальный узкий момент для будущего расширения диапазона.

**[P2] `Dashboard`: деление на ноль защищено, но `weights` может быть массивом из одного элемента**
Файл: `web/web/app/(app)/dashboard/page.tsx:90-99`
Проблема: `wMax - wMin || 1` защищает от деления на 0, если все веса одинаковые — это ок. Но `weights.length - 1 || 1` в знаменателе `x` даёт корректный fallback только для ровно одного элемента; если `weights` пуст (`getRecentActualWeights` теоретически может вернуть `[]` для нового пользователя без записей веса), `wMin`/`wMax` = `Math.min()`/`Math.max()` без аргументов = `Infinity`/`-Infinity`, что даст `NaN` в `spark`-строке точек polyline.
Воспроизведение: Новый пользователь без единой записи веса → `getRecentActualWeights` вернёт `[]` (см. `lib/data.ts:69` — `if (uid && uid !== "__legacy__") return []`) → `ws = []` → `Math.min(...[])` = `Infinity` → `spark` содержит `NaN,NaN ...` → SVG polyline рисуется пустым/некорректным (не крашится, но визуально сломанный график).
Рекомендация: Добавить guard: если `weights.length === 0`, не рендерить sparkline или показывать заглушку.

**[ПЕРЕПРОВЕРЕНО, УТОЧНЕНИЕ 2026-07-01] Эффект на самом деле мягче, но обнаружен связанный баг серьёзнее (см. новую секцию про онбординг ниже).** `weights.map(...)` на пустом массиве не итерируется вообще → `spark = ""` (пустая строка), а не `"NaN,NaN ..."` — `.map()` на `[]` возвращает `[]`, `.join(" ")` от `[]` даёт `""`. `<polyline points="">` не крашит и не рисует NaN в DOM — просто невидимая линия. Так что визуального «сломанного графика» нет, есть просто пустой график без данных (не блокер). **Однако:** для того же нового пользователя `currentWeight = await getCurrentWeight()` (используется на той же странице, для `weightDelta` и в теле карточки «вес») **не возвращает 0/null — а возвращает `seed.WEIGHT.currentKg` (66.75, реальный вес Марины)**, потому что `getCurrentWeight()` не содержит той же защиты `if (uid && uid !== "__legacy__") return []`, что стоит в `getRecentActualWeights`. Подробности — новый P0 в секции онбординга ниже.

**[P2] `sport_type` и `medication` global fallback наборы (`DEFAULT_SPORT_TYPES`, seed meds) — задокументированное поведение, но стоит явно тестировать онбординг нового пользователя**
Файл: `lib/data.ts:644-665`
Проблема: Не баг сам по себе, но в сочетании с P1-находкой про `getWorkoutTemplates` (не отдаёт сид-шаблоны новым пользователям) стоит явно прогнать сценарий "регистрация нового юзера → первый чек-ин → первая тренировка" целиком, потому что несколько функций (`getHabits`, `getMeds`, `getSportTypes`) уже корректно возвращают дефолты для нового `uid`, а `getWorkoutTemplates`/`getWeeklySchedule` — нет (инконсистентность поведения между похожими "первый запуск" сценариями).
Рекомендация: Пройти полный сценарий регистрации нового пользователя вручную (или через Илью/QA) с фокусом на `/training` и расписание.

Примечание к повторному аудиту: `getWorkoutTemplates` теперь отдаёт сид-шаблоны новым пользователям (см. P1 выше, исправлено). `getWeeklySchedule` (`lib/data.ts:429-438`) по-прежнему использует чистый `byUser` без `.or(null, uid)` — то есть инконсистентность, описанная здесь, сохраняется частично: шаблоны тренировок теперь "global shared" корректно, а недельное расписание — всё ещё нет. Не блокер (это P2, не трогали по заданию), но стоит иметь в виду при следующем проходе.

## Новые находки повторного аудита (2026-07-01)

**[P2] `deleteMed` — тихий no-op при попытке удалить чужую/не найденную запись**
Файл: `web/app/(app)/checkin/actions.ts:105-114`
Проблема: После фикса P0 (`byUser` фильтр добавлен), если `id` не принадлежит текущему пользователю, `delete()` с фильтром `.eq("app_user_id", uid)` вернёт `error: null`, но затронет 0 строк. Функция возвращает `{ ok: true }`, хотя ничего не удалилось.
Воспроизведение: Малореалистичный сценарий (нужно знать/угадать чужой id), но если он случится, UI покажет успешное удаление, а препарат останется на месте у владельца.
Рекомендация: Не блокер. Если нужна точность, можно проверять `count`/`data` после delete (`.select()`) и возвращать `{ ok: false, error: "Не найдено" }`, если ничего не удалено. Не критично для реального использования (обычный пользователь удаляет только свои id из своего же UI).

**[P2] `getWeeklySchedule` не получил тот же "global shared" фикс, что и `getWorkoutTemplates`**
Файл: `web/lib/data.ts:429-438`
Проблема: `getWorkoutTemplates` (соседняя функция) исправлена на `.or(app_user_id.is.null, app_user_id.eq.uid)`, а `getWeeklySchedule` остался на чистом `byUser`. Для нового пользователя без собственного расписания это вернёт пустой список расписания на неделю, хотя сид-расписание (`019_workout_types_by_dow.sql`/`006_training_exercises.sql`, `app_user_id IS NULL`) задумано как общий дефолт (судя по комментарию в коде и по аналогии с шаблонами).
Рекомендация: Если задумано, что недельное расписание тоже "global shared" по умолчанию — применить тот же `.or(...)` паттерн, что и в `getWorkoutTemplates`. Не проверял отдельно, входило ли это исправление в scope текущего коммита — похоже, что нет (это единственная асимметрия между двумя очень похожими функциями).

**[ПОДТВЕРЖДЕНО ПОВТОРНО] Перепроверено при тесте онбординга нового пользователя (2026-07-01, см. секцию ниже).** `web/lib/data.ts:425-434` — `getWeeklySchedule` по-прежнему чистый `byUser(...)` без `.or(app_user_id.is.null, ...)`. Для абсолютно нового зарегистрированного пользователя `/training/schedule` НЕ читает через эту функцию напрямую (там своя `getCalendarEvents` из `calendar_event`, другая таблица) — но `getWeeklySchedule` используется где-то ещё в дереве тренировок для дефолтного расписания недели; если это так и предполагалось «shared», баг остаётся неисправленным. Не апгрейжу до P1, т.к. эффект (пустое расписание вместо сид-дефолта) не крашит и не роняет данные — просто инконсистентность UX. Актуально на момент повторной проверки.

## Тест онбординга нового пользователя (2026-07-01)

Статический аудит кода (без запуска сервера, без создания реальных аккаунтов в БД). Трассировка пути «регистрация → 4 шага онбординга → первый заход на каждый основной экран» вручную, для гипотетического `uid` (новый UUID из `app_user`), у которого в БД физически нет ни одной строки ни в одной таблице кроме `app_user`. Проверены: `web/app/register/{page,actions}.tsx`, `web/app/onboarding/{page,actions}.ts`, `web/app/(app)/layout.tsx`, `web/lib/{auth,data,cycle,habits}.ts`, и главные экраны `dashboard`, `checkin`, `training(+schedule)`, `habits(+bulk)`, `medical`, `checkin/meds`, `insights(+report)`, `wearable`, `cycle`, `weight`.

### P0 — Блокеры

**[P0] Новый пользователь на дашборде/чек-ине/весе видит реальный вес Марины (66.75 кг) вместо своего**
Файл: `web/lib/data.ts:75-78` (`getCurrentWeight`), используется в `web/app/(app)/dashboard/page.tsx:77`, `web/app/(app)/checkin/page.tsx:32`, `web/app/(app)/weight/page.tsx:24`
Проблема:
```ts
export async function getCurrentWeight(): Promise<number> {
  const w = await getRecentActualWeights(8);
  return w.length ? w[w.length - 1].actual : seed.WEIGHT.currentKg;
}
```
Соседняя функция `getRecentActualWeights` для нового (не-legacy) пользователя явно возвращает `[]`, а не сид-данные Марины (`if (uid && uid !== "__legacy__") return [];` на `data.ts:70`) — то есть авторы намеренно решили не показывать чужие данные новым пользователям. Но `getCurrentWeight` не наследует этот guard: раз `w.length === 0`, она безусловно возвращает `seed.WEIGHT.currentKg` = `66.75` — жёстко захардкоженный реальный вес Марины из `web/lib/seed-data.ts:35`, независимо от того, легаси это пользователь или совершенно новый аккаунт.
Воспроизведение: Новый пользователь регистрируется → проходит онбординг → на шаге 4 либо вводит свой вес (тогда после `saveWeight` в БД появится строка `weight_entry` и баг не проявится), либо жмёт «Пропустить» (кнопка `handleSkipWeight` в `web/app/onboarding/page.tsx:98-108` вызывает только `completeOnboarding()`, не создавая никакой `weight_entry`) → на `/dashboard` в карточке «вес» отображается `66.75` (плюс `weightDelta = currentWeight - weightStartKg`, где `weightStartKg` берётся из `user.weightStartKg` — для нового юзера это `null` из БД, значит fallback на `WEIGHT_GOAL.startKg = 74.2`, тоже сид Марины) → пользователь видит чужие персональные данные о весе на первом же экране после регистрации. То же самое произойдёт на `/checkin` (`weightPlaceholder={weight}` — плейсхолдер в форме будет 66.75) и на `/weight` (крупная цифра "66.75" как «вес сегодня», плюс `fromStart`/`toGoal` относительно сид-констант `WEIGHT_GOAL.startKg=74.2`/`WEIGHT_GOAL.kg=54`, а не относительно `user.weightGoalKg`/`weightStartKg`, которые для нового юзера уже могли быть сохранены на шаге 4 онбординга).
Предположение о причине: При рефакторинге под мультиюзер (`getRecentActualWeights` получила guard "новый юзер → пустой список, не сид Марины"), забыли применить тот же guard к `getCurrentWeight`, хотя она вызывает `getRecentActualWeights` и явно рассчитана на тот же сценарий "нет своих записей веса".
Рекомендация: В `getCurrentWeight` заменить безусловный `seed.WEIGHT.currentKg` на условный fallback (как в других функциях `data.ts`): если `uid` — новый (не legacy) и записей нет, вернуть `null`/`0`, а не сид Марины; вызывающему коду (`dashboard`, `checkin`, `weight`) явно обработать «веса ещё нет» (например, показать «—» вместо цифры). Дополнительно: `web/app/(app)/weight/page.tsx` вообще не вызывает `getCurrentUser()` и всегда считает `fromStart`/`toGoal` от `WEIGHT_GOAL.startKg`/`WEIGHT_GOAL.kg` (сид-константы) — нужно аналогично `dashboard/page.tsx:86-87` подставлять `user.weightGoalKg ?? WEIGHT_GOAL.kg` и `user.weightStartKg ?? WEIGHT_GOAL.startKg`.

**[P0] `lib/habits.ts` (`fetchByDates`) читает `daily_log.habits_done` без фильтра по пользователю — утечка на `/habits` и `/habits/bulk`**
Файл: `web/lib/habits.ts:19-32` (`fetchByDates`, используется `getHabitDays` и `getHabitMonth`)
Проблема:
```ts
async function fetchByDates(dates: string[]): Promise<HabitDay[]> {
  const db = supabaseAdmin();
  if (!db) return dates.map((d) => ({ dateISO: d, label: labelFor(d), done: [] }));
  const { data } = await db
    .from("daily_log")
    .select("log_date, habits_done")
    .in("log_date", dates);
  ...
}
```
Нет ни `byUser`, ни какого-либо `app_user_id`-фильтра вообще — это прямой запрос ко всей таблице `daily_log` по датам, без учёта владельца. Это ровно тот класс бага, что был найден и исправлен в `saveBulkHabits` (см. P0 выше в файле), но здесь — в чтении, и, судя по всему, не был замечен при том же рефакторинге, потому что это отдельный модуль (`lib/habits.ts`, не `lib/data.ts`).
Воспроизведение: Новый пользователь открывает `/habits` (матрица привычек) в первый же день после онбординга. `getHabitMonth(targetMonth)` → `fetchByDates` вернёт `habits_done` **из строк daily_log любого пользователя** (включая сид-данные Марины) на пересекающихся датах месяца. UI (`web/app/(app)/habits/page.tsx:112`: `d.done.includes(habit)`) отметит галочкой день, если совпадающее название привычки встретилось у **любого** пользователя в этот день — например, если у нового пользователя есть привычка «Вода 1.5л+» (из дефолтного списка онбординга) и у Марины в её `daily_log` в тот же календарный день тоже отмечена «Вода 1.5л+», матрица нового пользователя покажет выполненный чек-ин, хотя сам пользователь ничего не логировал. То же самое влияет на `/habits/bulk` (там `getHabitMonth` тоже используется для отображения формы массового заполнения) — вплоть до того, что `BulkForm` может показать предзаполненные (чужие) отметки за прошлые дни.
Предположение о причине: `lib/habits.ts` — самостоятельный модуль, использующий "сырой" `supabaseAdmin()` без импорта паттерна `byUser`/`getAppUserId` из `lib/data.ts`; похоже, не попал в область мультиюзер-рефакторинга, хотя читает ту же таблицу `daily_log`, что и `getMonthHabitStats`/`getDailyLog` в `data.ts` (которые фильтрацию имеют).
Рекомендация: Добавить в `lib/habits.ts` тот же паттерн `getAppUserId`/`byUser`, что в `lib/data.ts` (можно даже импортировать оттуда, если экспортировать хелперы), и применить к запросу в `fetchByDates`. Приоритет P0, а не P1, потому что это утечка персональных данных о состоянии здоровья/привычках между пользователями, причём срабатывает уже в первый день использования — не гипотетический edge case.

### P1 — Важные баги

**[P1] Длина цикла, введённая на шаге 2 онбординга, нигде не сохраняется и не используется**
Файл: `web/app/onboarding/actions.ts:19-28` (`saveCycle`), `web/app/onboarding/page.tsx:166-174` (UI шага 2)
Проблема: На шаге 2 онбординга пользователь вводит `cycleLen` (число от 21 до 45, дефолт 28) в поле «Длина цикла (дни)». `handleNext()` вызывает `saveCycle(lastPeriod, cycleLen)` (`page.tsx:78`). Но в `actions.ts`:
```ts
export async function saveCycle(lastPeriodDate: string, _cycleLength: number) {
  const uid = await getUid();
  const db = supabaseAdmin();
  if (!db) throw new Error("DB unavailable");
  await db.from("cycle_start").upsert(
    { app_user_id: uid, start_date: lastPeriodDate },
    { onConflict: "app_user_id,start_date" }
  );
}
```
Параметр явно переименован в `_cycleLength` (со знаком подчёркивания — типичный маркер "намеренно не используется") и нигде не используется. Таблица `cycle_start` хранит только `start_date`, длина цикла нигде не персистится (ни в `app_user`, ни отдельной колонкой).
Воспроизведение: Пользователь с реальным циклом 32 дня вводит на онбординге "32". Значение никуда не пишется. `getCurrentCycle` (`web/lib/cycle.ts:80-96`) для пользователя с одной записью `cycle_start` вычисляет `stats = cycleStats([oneDate])` → `lengths = []` (нечего сравнивать, нужно ≥2 старта) → `recent = []` → `n = 1` (fallback) → `avg = 0/1 = 0` → `len = Math.round(0) || 28 = 28`. То есть независимо от того, что ввёл пользователь на шаге 2 (21–45 дней), движок цикла всегда стартует с дефолтных 28 дней, пока не накопится ≥2 реальных менструации. Это не крашит, но вводит пользователя в заблуждение — поле выглядит так, будто оно на что-то влияет, но эффекта нет вообще, даже временного.
Предположение о причине: Похоже, `cycle_start`/`getCurrentCycle` изначально проектировались на "адаптивный" расчёт длины цикла из истории (комментарий в `cycle.ts:1`: «адаптивный прогноз, а не фиксированные 28 дней»), и разработчик сознательно решил не хранить ручной ввод длины — но тогда поле в UI онбординга вводит в заблуждение: обещает персонализацию, которой не будет, пока не накопится история.
Рекомендация: Либо (а) убрать поле «длина цикла» с шага 2 онбординга совсем (раз оно ни на что не влияет и движок всё равно всё вычисляет сам из истории), либо (б) реально использовать введённое значение как стартовый дефолт для `getCurrentCycle` до накопления истории — например, сохранить `cycle_length_default` в `app_user` и передавать его как fallback вместо жёстко зашитого `28` в `cycle.ts:83` (`const len = Math.round(stats.avgLength) || 28;` → `|| (user.cycleLengthDefault ?? 28)`).

### Проверено и не является багом (эффект слабее, чем можно было предположить)

- **Dashboard sparkline при `weights = []`**: `weights.map(...)` на пустом массиве не итерируется, `spark = ""` (не `"NaN,NaN"`), `<polyline points="">` рендерится без графика, без визуального слома и без ошибок в консоли. Не блокер (см. уточнение к старому P2 выше). Обнаружен рядом более серьёзный баг — см. P0 про `getCurrentWeight`.
- **`getMeds`/`getHabits`/`getSportTypes` для нового `uid`**: `getHabits`/`getMeds` корректно возвращают `[]` для нового пользователя (комментарий в коде `data.ts:167` подтверждает намеренность — «Real users with no habits yet get an empty list, not Marina's seed data»); UI `/checkin` и `/habits` корректно обрабатывает пустые списки (нет привычек/препаратов — просто пустая форма/таблица, без крашей). `getSportTypes` идёт дальше и явно отдаёт `DEFAULT_SPORT_TYPES` (не сид Марины, а нейтральный дефолтный набор) — корректное поведение.
- **`/wearable`, `/cycle`, `/insights`, `/medical` для пустых данных**: все проверенные Math.min/Math.max используют safe fallback (`Math.max(...arr, 1)` или `arr.length ? ... : constant`), пустые состояния показывают явный UI-текст («Документов пока нет», «данные кольца» и т.п.), крашей не найдено.
- **`getWorkoutTemplates`**: подтверждено, что фикс на `.or(app_user_id.is.null, app_user_id.eq.uid)` работает — новый пользователь на `/training` получит сид-шаблоны тренировок при создании новой тренировки.
- **`(app)/layout.tsx` редирект-логика**: `if (!user) redirect("/login")` → `if (!user.onboardingDone) redirect("/onboarding")` — линейная, без риска зацикливания. `onboarding/page.tsx` после `completeOnboarding()` делает `router.push("/")`, а не `/dashboard` напрямую — не проверял, куда редиректит `/` (вне scope этой сессии, но стоит перепроверить отдельно, что `/` не ведёт обратно в layout с устаревшим `onboardingDone` из-за Router Cache — клиентский `router.push` может закешировать старый ответ layout на 30 сек, если `onboarding_done` не проставился в БД до пуша; `completeOnboarding()` — await, так что порядок операций корректный, гонки нет).

### Итог по пути нового пользователя

Регистрация → онбординг (4 шага, включая «Пропустить» на весе) проходит без крашей и redirect-петель. Но на **первом же экране после онбординга** (`/dashboard`, если вес не был введён на шаге 4) и на `/checkin`, `/weight` новый пользователь увидит чужие персональные данные о весе (66.75 кг — реальный вес Марины) вместо пустого состояния. На `/habits` и `/habits/bulk` в первый месяц использования возможна утечка чужих отметок о привычках на пересекающихся календарных датах. Оба — P0, требуют фикса перед тем как звать реальных вторых пользователей в продакшн.

## Третий раунд аудита — 2026-07-01 (после миграции 032)

Метод: (1) статическое чтение всех заявленных изменённых файлов (`web/lib/data.ts`, `web/lib/habits.ts`, `web/lib/cycle.ts`, `web/lib/auth.ts`, `web/app/onboarding/actions.ts`, `web/app/onboarding/page.tsx`, 5 экранов с `getCurrentCycle`, `web/app/(app)/weight/page.tsx`, `web/app/(app)/weight/weight-input.tsx`, `web/app/(app)/checkin/checkin-form.tsx`, `web/app/(app)/checkin/actions.ts`, миграция `032_avg_cycle_length.sql`); (2) **живой сквозной тест** на локальном dev-сервере (`npm run dev`, ветка `main`, коммит `31d98cc`) с реальной регистрацией нового пользователя в проде-БД (Supabase), полным прохождением онбординга (4 шага, длина цикла введена как 32 — не дефолт 28, чтобы поймать любые места, где дефолт зашит жёстко), и проверкой всех 5 экранов (`/dashboard`, `/checkin`, `/weight`, `/training`, `/training/schedule`, `/habits`). Тестовый аккаунт и его данные (`app_user`, `cycle_start`, `habit`) удалены из БД после теста.

Попутно: `.claude/launch.json` в корне репозитория указывал на устаревший путь `cd web/web && npm run dev` (остался от структуры до консолидации `b790ca5` — «chore: consolidate nested repo into single git tree»). Поправил на `cd web && npm run dev`, иначе preview-сервер не поднимался в принципе. Не баг продукта, но фиксирую — если у Марины или другого агента тоже не запускается локальный дев-сервер, вот причина.

### A. Трассировка avgCycleLength — по большей части ИСПРАВЛЕНО ✓, но найден один пропущенный экран

**[ИСПРАВЛЕНО ✓] Миграция 032, `auth.ts`, `saveCycle`, 5 мест `getCurrentCycle`**
Проверено построчно: `supabase/migrations/032_avg_cycle_length.sql` — простой `ALTER TABLE app_user ADD COLUMN IF NOT EXISTS avg_cycle_length int`, применена (подтверждено — SELECT из `app_user` в проде вернул колонку с реальным значением для тестового юзера). `web/lib/auth.ts:83,86,96` — `AppUser.avgCycleLength`, `USER_SELECT` включает `avg_cycle_length`, `rowToUser` мапит корректно `(data.avg_cycle_length as number | null) ?? null`. `web/app/onboarding/actions.ts:19-31` — `saveCycle(lastPeriodDate, cycleLength)` реально пишет оба поля: `cycle_start` upsert + `app_user.avg_cycle_length = cycleLength` отдельным update. Живой тест подтвердил: ввёл на онбординге длину цикла **32** (не дефолт), в БД после завершения онбординга `app_user.avg_cycle_length = 32` — записалось верно. Все 5 вызовов `getCurrentCycle` (`layout.tsx:16`, `dashboard/page.tsx:79`, `checkin/page.tsx:36`, `weight/page.tsx:29`, `training/page.tsx:170`) единообразно передают `user?.avgCycleLength ?? 28` третьим параметром. Живой тест: на `/dashboard`, `/checkin`, `/weight`, `/training` фаза цикла показана одинаково («Менструальная», день 1) и «≈32 дня до месячных» на дашборде — использует именно введённое значение, а не захардкоженные 28. Консистентность между экранами подтверждена на реальных данных, не только по коду.

**[P2 — новая находка] `getWeeklySchedule` в `lib/data.ts:422-431` — мёртвый код, никем не используется**
Файл: `web/lib/data.ts:422-431`
Проблема: В прошлом раунде это было отмечено как незакрытая асимметрия с `getWorkoutTemplates` (тот получил `.or(app_user_id.is.null, ...)`, а `getWeeklySchedule` — нет). Перепроверил grep по всему `app/` и `lib/`: `getWeeklySchedule` **не импортируется и не вызывается нигде**. Экран `/training/schedule` использует отдельный `getCalendarEvents` из `./actions.ts` (таблица `calendar_event`, не `weekly_schedule`) — это архитектурно другой путь, введённый позже (миграции 025-026, судя по истории коммитов). Живой тест подтвердил: `/training/schedule` для нового пользователя показывает пустой календарь без крашей, без завязки на `weekly_schedule`/`getWeeklySchedule` вообще.
Рекомендация: Не блокер и не баг поведения (эффекта на пользователя нет — функция мертва). Но стоит либо удалить `getWeeklySchedule` и связанный тип `ScheduleDay` как мёртвый код (если `weekly_schedule` таблица тоже не используется активно), либо явно задокументировать, что это устаревший путь, оставленный для обратной совместимости. Понижаю приоритет с P1/P2-observation прошлого раунда до чистого P2 «мёртвый код», так как реального пользовательского риска нет.

### B. Новые находки в этом раунде

**[P1] `/weight` показывает цель и старт веса Марины (54 кг / 74.2 кг) вместо «нет данных» для нового пользователя**
Файл: `web/app/(app)/weight/page.tsx:30-31,44,63`
Проблема:
```ts
const fromStart = current != null ? +(current - WEIGHT_GOAL.startKg).toFixed(1) : null;
const toGoal = current != null ? +(current - WEIGHT_GOAL.kg).toFixed(1) : null;
...
план и факт · цель {WEIGHT_GOAL.kg} кг         // строка 44
...
от старта {WEIGHT_GOAL.startKg}                 // строка 63
```
`WEIGHT_GOAL` — это захардкоженный сид-объект (`web/lib/data.ts:86-90`: `kg: seed.WEIGHT.goalKg`, `startKg: seed.WEIGHT.startKg` — реальные цифры Марины, 54 и 74.2). Экран **не вызывает `getCurrentUser()` вообще** и нигде не подставляет `user.weightGoalKg`/`user.weightStartKg`, хотя ровно эта подстановка уже сделана на `dashboard/page.tsx:85-86` (`user?.weightGoalKg ?? WEIGHT_GOAL.kg`, `user?.weightStartKg ?? WEIGHT_GOAL.startKg`) — причём именно это было явной рекомендацией в P0-находке предыдущего раунда («Дополнительно: `web/app/(app)/weight/page.tsx` вообще не вызывает `getCurrentUser()`... нужно аналогично dashboard/page.tsx:86-87 подставлять `user.weightGoalKg ?? WEIGHT_GOAL.kg`»). Похоже, фикс применили только к `getCurrentWeight()` (сам вес теперь `null`-safe), но параллельную часть той же рекомендации — про цель/старт веса на `/weight` — не перенесли.
Воспроизведение (подтверждено вживую): зарегистрировал нового пользователя, прошёл онбординг, пропустил шаг веса. Зашёл на `/weight` — заголовок страницы показывает **«план и факт · цель 54 кг»** и в карточке (при вводе любого веса) будет показано «от старта 74.2» — обе цифры принадлежат Марине, не имеют отношения к новому пользователю, который либо вообще не указал цель, либо указал другую на шаге 4 онбординга (если бы не пропустил). Если пользователь ввёл на онбординге свою цель (например, 60 кг) — она сохранится в `user.weightGoalKg`, но `/weight` её всё равно проигнорирует и покажет «54» из сида.
Понижаю до P1 (не P0), потому что это не выдаёт приватные факты о теле Марины напрямую (как было с `currentWeight`=66.75 в прошлом раунде) — это её целевые/стартовые цифры без явного «это чужие данные», но всё равно вводит пользователя в заблуждение относительно собственной цели и может использоваться как некорректный референс.
Рекомендация: В `weight/page.tsx` добавить `getCurrentUser()` в `Promise.all`, заменить `WEIGHT_GOAL.kg`/`WEIGHT_GOAL.startKg` на `user?.weightGoalKg ?? WEIGHT_GOAL.kg` / `user?.weightStartKg ?? WEIGHT_GOAL.startKg` во всех 3 местах использования (заголовок, карточка «от старта», `WeightChart goalKg`).

**[P2] Заголовок цикла на дашборде («ЦИКЛ · N ДН») показывает 0 для нового пользователя вместо введённой длины 32**
Файл: `web/app/(app)/dashboard/page.tsx:80`
Проблема:
```ts
const length = Math.round(c.stats.avgLength);
```
`c.stats` — результат `cycleStats(sorted)` (`lib/cycle.ts:39-51`), которая вычисляет `avgLength` из **разницы между реальными `cycle_start` записями** (`lengths.push(daysBetween(...))` в цикле от i=1). Для нового пользователя с ровно 1 записью `cycle_start` (внесённой на онбординге) цикл `for (let i = 1; i < sorted.length; i++)` не выполняется ни разу (`sorted.length === 1`) → `lengths = []` → `avg = 0/1 = 0` → `length = Math.round(0) = 0`.
Это отдельная переменная от `len` внутри самого `getCurrentCycle` (`lib/cycle.ts:83`: `const len = Math.round(stats.avgLength) || defaultLength`) — там есть `|| defaultLength` fallback на `avgCycleLength`, и именно поэтому «≈32 дня до месячных» на том же дашборде показывается верно. Но `dashboard/page.tsx:80` берёт `c.stats.avgLength` напрямую, без доступа к `defaultLength`/`avgCycleLength`, и без своего `|| 28`/`|| avgCycleLength` fallback.
Воспроизведение (подтверждено вживую): новый пользователь, ввёл длину цикла 32 на онбординге, только одна запись `cycle_start`. На `/dashboard` в верхнем левом углу циферблата написано **«ЦИКЛ · 0 ДН»**, при этом чуть ниже в том же виджете корректно «≈ 32 дня до месячных». Визуально сбивающее с толку противоречие на одном экране: заголовок утверждает, что цикл 0 дней, а прогноз следующих месячных — через 32 дня.
Рекомендация: Использовать тот же fallback, что и внутри `getCurrentCycle`: `const length = Math.round(c.stats.avgLength) || (user?.avgCycleLength ?? 28);` — либо ещё проще, `getCurrentCycle` мог бы возвращать уже посчитанную эффективную длину (`len`) в объекте `CycleInfo`, чтобы вызывающему коду не приходилось дублировать fallback-логику. Не блокер (не крашит, не искажает прогноз месячных, только сбивающий с толку заголовок), но это ровно тот тип "console.log-уровня" несостыковки, который бросается в глаза новому пользователю в первую же секунду использования — рекомендую поднять до P1 при следующем спринте, если будет время.

### C. Проверено и подтверждено рабочим (без замечаний)

- **`web/lib/habits.ts` (`fetchByDates`)** — фикс P0 из прошлого раунда подтверждён и статически, и вживую: добавлен `byUser`, запрос теперь фильтрует по `app_user_id`. Живой тест: новый пользователь с привычкой «Вода 1.5л+» на `/habits` за июль 2026 видит **все 31 день пустыми** (проверено программным обходом DOM, ни одна ячейка не отмечена) — никакой утечки отметок Марины на пересекающихся датах, хотя у Марины наверняка есть отметки той же привычки в этом месяце в сиде.
- **`getCurrentWeight()` null-safety** — код (`web/lib/data.ts:78-84`) и живой тест сходятся: новый пользователь без записей веса на `/dashboard`, `/checkin` видит «—»/пустой плейсхолдер, не 66.75 Марины. `weightDelta` на дашборде (`currentWeight != null ? ... : null`) корректно даёт `null` → «нет данных» / «введи вес →», а не крашится и не считает `undefined - number`.
- **`checkin/checkin-form.tsx`, `weight/weight-input.tsx`** — оба принимают `number | null` и используют `placeholder != null ? ... : "..."` паттерн корректно; ни один компонент не упал на `null` при живом тесте (поля показывали пустой плейсхолдер «кг»/«введи вес», не `null`/`NaN` в тексте).
- **`checkin/actions.ts` → `deleteMed`** — теперь делает `byUser(...).select("id")`, проверяет `data.length === 0` и возвращает честную ошибку «Препарат не найден» вместо молчаливого `ok: true`. Прочитан построчно, логика верна (P2 из прошлого раунда закрыт).
- **`register/actions.ts`** — `redirect()` по-прежнему вызывается один раз в конце функции, вне try/catch. Полный путь регистрации нового пользователя (включая нормальный, не error-кейс) отработал вживую без сбоев: `POST /register → 303 → GET /onboarding`.
- **Consistency между 5 экранами по фазе/дню цикла** — подтверждено вживую, не только по коду: `/dashboard`, `/checkin`, `/weight`, `/training` для одного и того же нового пользователя (1 запись `cycle_start`, `avg_cycle_length=32`) показали одинаково «Менструальная», день 1.
- **`/training`, `/training/schedule`, `/habits`, `/checkin`** для полностью нового пользователя без единой тренировки/чек-ина — не крашятся, показывают корректные нулевые/пустые состояния («0 / 150», «Нет препаратов — добавь ниже», пустой календарь, пустая матрица привычек кроме выбранной на онбординге).

### Методологическое замечание

При живом тесте обнаружилась любопытная деталь инструментария (не бага продукта): синтетические клики через `preview_click` **не триггерили** ни submit формы регистрации (`<form action={register}>` server action), ни `onClick`-обработчики кнопок онбординга (`handleNext`) — ни один переход не происходил, несмотря на «Successfully clicked». Сработало только через `form.requestSubmit()` и через полный набор синтетических событий (`pointerdown/mousedown/pointerup/mouseup/click` с `dispatchEvent`) вместо одиночного клика. Первая попытка регистрации из-за этого молча не создала пользователя, и я едва не отчитался о ложном "всё ок работает как раньше" на основании невалидного теста — важно на будущее: **всегда проверять сетевые запросы/логи сервера или прямой SQL к БД после каждого шага живого теста**, а не доверять только визуальному снапшоту страницы, потому что клик может не сработать молча.

### Резюме третьего раунда

Все P0 из прошлого раунда (утечка веса Марины, утечка привычек через `lib/habits.ts`) подтверждены закрытыми — и статически, и на живых данных с реальной регистрацией. `avgCycleLength` полностью протрассирован от онбординга до всех 5 экранов и работает консистентно на живом тесте. Найдено 2 новых дефекта, оба не блокеры: P1 (цель/старт веса Марины всё ещё просвечивают на `/weight` — эта же строка кода была явно указана в прошлой рекомендации, но применена только частично) и P2 (сбивающий с толку «0 дней» в заголовке цикла на дашборде при 1 записи cycle_start). `getWeeklySchedule` подтверждён как мёртвый код, не несущий риска. Онбординг нового пользователя в целом работоспособен и не содержит блокеров, но `/weight` для нового пользователя до первого фикса не готов к показу реальным вторым пользователям — цифры Марины (54/74.2 кг) будут видны на этом экране всем новым аккаунтам.
